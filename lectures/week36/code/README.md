# PageRank algorithm

A simple implementation of the PageRank algorithm used in the interactive lecture.

Initialize the Python environment by

```bash
uv sync
```

and run the code by

```bash
uv run pagerank.py
```
